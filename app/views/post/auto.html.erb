<h3><strong>自動投稿(BOT)</strong></h3>
<p class="text-muted">一定の時間間隔で自動投稿を行います。</p><br>
<button type="button" name="button" class="btn btn-success addauto">自動投稿を追加</button><br><br>
<form class="addautoform">
  <div class="form-group">
    <label for="text">投稿文</label>
    <textarea class="form-control" id="text" rows="10"></textarea>
  </div>
  <div class="form-group">
    <label for="File">画像</label>
    <input type="file" class="form-control-file" id="File">
  </div>
  <div class="form-check form-check-inline">
    <input class="form-check-input" type="checkbox" id="inlineCheckbox1" value="option1" checked>
    <label class="form-check-label">
      Twitter
      <small class="text-muted">
        <% if @twitterActive %>
        (<%= @twitterActive.profile_name %>)
        <% else %>
        (未設定)
        <% end %>
      </small>
    </label>
  </div>
  <div class="form-check form-check-inline">
    <input class="form-check-input" type="checkbox" id="inlineCheckbox2" value="option2" checked>
    <label class="form-check-label">
      Instagram
      <small class="text-muted">
        <% if @instagramActive %>
        (<%= @instagramActive.profile_name %>)
        <% else %>
        (未設定)
        <% end %>
      </small>
    </label>
  </div>
  <div class="form-check form-check-inline">
    <input class="form-check-input" type="checkbox" id="inlineCheckbox3" value="option3" checked>
    <label class="form-check-label">
      Facebook
      <small class="text-muted">
        <% if @facebookActive %>
        (<%= @facebookActive.profile_name %>)
        <% else %>
        (未設定)
        <% end %>
      </small>
    </label>
  </div><br><br>
  <button type="submit" class="btn btn-primary">追加する</button><br><br>
</form>
<div class="card">
  <div class="card-header"><strong>投稿間隔と順番を選択</strong></div>
  <div class="card-body">
    <form>
      <div class="row">
        <div class="form-group col-3">
          <label for="postrange">投稿間隔</label>
          <select class="custom-select">
            <option >停止</option>
            <option value="1" selected>1時間に1回</option>
            <option value="2">2時間に1回</option>
            <option value="3">3時間に1回</option>
            <option value="1">4時間に1回</option>
            <option value="2">5時間に1回</option>
            <option value="3">6時間に1回</option>
          </select>
        </div>
        <div class="form-group col-3">
          <label for="postorder">投稿順</label>
          <select class="custom-select">
            <option value="1" selected>更新が新しい順</option>
            <option value="2">更新が古い順</option>
            <option value="3">ランダム(重複有)</option>
            <option value="1">ランダム(重複無)</option>
            <option value="2">優先度順</option>
          </select>
        </div>
      </div>
      <button type="submit" class="btn btn-primary">変更する</button>
    </form>
  </div>
</div><br>
<div class="card">
  <div class="card-header"><strong>自動投稿一覧</strong></div>
  <div class="card-body">

  </div>
</div>
<script>
  $('.addautoform').hide();
  $('.addauto').click(function () {
    $('.addautoform').fadeToggle();
  })
</script>
